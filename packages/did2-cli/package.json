{
  "name": "did2-cli",
  "type": "module",
  "version": "0.0.1",
  "license": "MIT",
  "bin": {
    "did2-cli": "dist/index.js"
  },
  "scripts": {
    "dev": "bun --watch run index.ts",
    "all": "bun test && bun build index.ts --outdir dist"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "did2-lib": "workspace:*"
  }
}
